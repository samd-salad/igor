# Client dependencies for Raspberry Pi
# Pinned to compatible-release ranges (~=) to accept patch updates only.
pyaudio~=0.2.14
numpy~=2.3.5
requests~=2.32.5
flask~=3.1.1
waitress~=3.0.2
# openwakeword installed via setup_client.sh with --no-deps to skip tflite-runtime
# (tflite has no Python 3.13 build; we use inference_framework="onnx" so it's unneeded)
onnxruntime~=1.22.0
scipy~=1.15.3
scikit-learn~=1.7.1
tqdm~=4.67.1
pydantic~=2.12.5
